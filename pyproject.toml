[project]
name = "framecraft"
version = "0.1.0"
description = "Create polished demo videos from screenshots and scene descriptions. HTML scenes + headless Chrome + ffmpeg + TTS."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "edge-tts>=7.2.8",
    "mcp>=1.26.0",
    "playwright>=1.58.0",
]
